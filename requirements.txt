# Core dependencies -- everything needed to run the SQLite-backed API,
# pipeline, index engine, ML, and tests. This is all you need for local
# development and the demo (see README.md "Fastest way to see it work").
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pandas==2.2.3
numpy==2.1.1
scikit-learn==1.5.2
apscheduler==3.10.4
python-dotenv==1.0.1
pytest==8.3.3
httpx==0.27.2
